---
phase: 11-cli-distribution-and-homebrew-packaging
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - package.json
  - shared/package.json
  - server/package.json
  - server/src/cli.ts
  - server/src/start-server.ts
  - server/tests/cli/standalone-assets.test.ts
  - server/tests/cli/package-artifact.test.ts
  - scripts/verify-cli-package.mjs
autonomous: true
requirements:
  - CLI-01
  - CLI-03
  - DIST-01

must_haves:
  truths:
    - "Root build succeeds and no longer references a missing shared build script"
    - "The packaged artifact contains server/dist/cli.js and client/dist/index.html"
    - "The CLI resolves packaged static assets relative to the built module, not process.cwd()"
    - "A smoke script can launch the packed artifact against an arbitrary temp notes directory"
  artifacts:
    - path: "package.json"
      provides: "publishable root package metadata and bin/files/prepack contract"
    - path: "shared/package.json"
      provides: "shared workspace build script"
    - path: "server/package.json"
      provides: "server build script"
    - path: "scripts/verify-cli-package.mjs"
      provides: "packed-artifact smoke verification"
  key_links:
    - from: "package.json"
      to: "server/dist/cli.js"
      via: "bin.marky"
      pattern: "\"marky\": \"server/dist/cli.js\""
    - from: "server/src/cli.ts"
      to: "client/dist/index.html"
      via: "staticDir resolution relative to import.meta.url"
      pattern: "client/dist"
---

<objective>
Make Phase 11 actually distributable: fix the build pipeline, package the built server/client into the root `marky` artifact, and verify the packed CLI works from an arbitrary folder.

Purpose: Turn the CLI implementation into a real installable artifact.
Output: Working root build + `npm pack` contract + smoke verification script.
</objective>

<execution_context>
@/Users/romankarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-CONTEXT.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-RESEARCH.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-VALIDATION.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix build and packaged static-asset contract</name>
  <files>package.json, shared/package.json, server/package.json, server/src/cli.ts, server/src/start-server.ts</files>
  <read_first>
    - package.json
    - shared/package.json
    - server/package.json
    - server/src/cli.ts
    - server/src/start-server.ts
    - server/tests/cli/standalone-assets.test.ts
    - server/tests/cli/package-artifact.test.ts
  </read_first>
  <action>
Apply these exact package/build changes:

In root `package.json`:
- set `"private": false`
- add `"bin": { "marky": "server/dist/cli.js" }`
- add `"files": ["server/dist", "client/dist", "shared/dist", "package.json", "README.md"]`
- replace `"build"` with `npm run build --workspace=shared && npm run build --workspace=server && npm run build --workspace=client`
- add `"pack:dry": "npm pack --json --dry-run"`
- add `"prepack": "npm run build"`

In `shared/package.json`:
- add `"build": "tsc -p tsconfig.json --emitDeclarationOnly --outDir dist"`
- add `"types": "dist/types.d.ts"` if the emitted file name matches, or another exact emitted declaration path

In `server/package.json`:
- add `"build": "tsc -p tsconfig.json"`

In `server/src/cli.ts` or `server/src/start-server.ts`:
- resolve packaged `client/dist` using `fileURLToPath(import.meta.url)` and `path.resolve(..., '../../client/dist')`
- pass that resolved directory into `startMarkyServer`
- throw a clear error mentioning `client/dist` when the directory is missing
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm run build && cd server && npx vitest run tests/cli/standalone-assets.test.ts tests/cli/package-artifact.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - root `package.json` contains `"private": false`
    - root `package.json` contains `"marky": "server/dist/cli.js"`
    - root `package.json` contains `"pack:dry": "npm pack --json --dry-run"`
    - `shared/package.json` contains `"build": "tsc -p tsconfig.json --emitDeclarationOnly --outDir dist"`
    - `server/package.json` contains `"build": "tsc -p tsconfig.json"`
    - `server/src/cli.ts` or `server/src/start-server.ts` contains `fileURLToPath(import.meta.url)`
    - `server/src/cli.ts` or `server/src/start-server.ts` contains `../../client/dist`
  </acceptance_criteria>
  <done>The monorepo builds, packaged static asset resolution is module-relative, and the root package is publishable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add packed-artifact smoke verification</name>
  <files>server/tests/cli/standalone-assets.test.ts, server/tests/cli/package-artifact.test.ts, scripts/verify-cli-package.mjs</files>
  <read_first>
    - server/tests/cli/standalone-assets.test.ts
    - server/tests/cli/package-artifact.test.ts
    - package.json
  </read_first>
  <action>
Create `scripts/verify-cli-package.mjs` that:
- runs `npm pack --json` in repo root
- extracts the tarball into a temp directory
- creates a temp notes directory with at least one markdown file
- launches the packed CLI with `node package/server/dist/cli.js --no-open <temp-notes-dir>`
- waits for stdout containing `http://127.0.0.1:`
- requests `/api/files` from that URL and asserts the temp markdown file is listed
- shuts the process down cleanly

Make `server/tests/cli/package-artifact.test.ts` verify the smoke script exists and can be invoked without hardcoded repo-only paths.

Do not make the smoke script depend on Homebrew or globally installed binaries; it must validate the raw packed artifact only.
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm run pack:dry && node scripts/verify-cli-package.mjs</automated>
  </verify>
  <acceptance_criteria>
    - file `scripts/verify-cli-package.mjs` exists
    - `scripts/verify-cli-package.mjs` contains `npm pack --json`
    - `scripts/verify-cli-package.mjs` contains `node package/server/dist/cli.js --no-open`
    - `server/tests/cli/package-artifact.test.ts` contains `scripts/verify-cli-package.mjs`
    - packed smoke flow asserts `/api/files`
  </acceptance_criteria>
  <done>The packaged CLI can be verified from a temp notes directory without relying on the source checkout layout.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm run build`
2. `cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm run pack:dry`
3. `cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && node scripts/verify-cli-package.mjs`
4. `cd /Users/romankarski/Documents/Documents_v2/Projects/Marky/server && npx vitest run tests/cli/standalone-assets.test.ts tests/cli/package-artifact.test.ts --reporter=verbose`
</verification>

<success_criteria>
- Root build succeeds
- `npm pack --json --dry-run` matches the expected CLI artifact contract
- Smoke script launches the packed CLI against an arbitrary notes folder
- Static assets are resolved relative to the built package, not cwd
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-distribution-and-homebrew-packaging/11-03-SUMMARY.md`
</output>

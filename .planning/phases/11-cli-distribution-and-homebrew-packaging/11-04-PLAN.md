---
phase: 11-cli-distribution-and-homebrew-packaging
plan: 04
type: execute
wave: 3
depends_on:
  - 11-03
files_modified:
  - .github/workflows/release-cli.yml
  - packaging/homebrew/marky.rb
  - README.md
  - scripts/verify-homebrew-install.mjs
autonomous: false
requirements:
  - CLI-01
  - CLI-04
  - CLI-05
  - DIST-01

must_haves:
  truths:
    - "Release automation produces and uploads the npm-pack tarball artifact"
    - "A Homebrew formula exists and installs the packaged CLI with a Node dependency"
    - "README documents install flows for direct package use and Homebrew"
    - "A blocking human verification step confirms the actual Homebrew install flow"
  artifacts:
    - path: ".github/workflows/release-cli.yml"
      provides: "release artifact build/upload automation"
    - path: "packaging/homebrew/marky.rb"
      provides: "Homebrew formula consuming release tarball"
    - path: "README.md"
      provides: "user-facing install and usage documentation"
    - path: "scripts/verify-homebrew-install.mjs"
      provides: "pre-flight verification harness or checklist helper"
  key_links:
    - from: "packaging/homebrew/marky.rb"
      to: "server/dist/cli.js"
      via: "wrapper script executes packaged CLI"
      pattern: "server/dist/cli.js"
    - from: ".github/workflows/release-cli.yml"
      to: "package.json"
      via: "npm pack release artifact generation"
      pattern: "npm pack"
---

<objective>
Finish Phase 11 for real users: create release automation, add the Homebrew formula, document the install flow, and complete a blocking verification checkpoint for the packaged distribution.

Purpose: Turn the packaged CLI into a distributable product, not just a local build artifact.
Output: Release workflow, Homebrew formula, install docs, and human-approved distribution flow.
</objective>

<execution_context>
@/Users/romankarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-CONTEXT.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-RESEARCH.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-VALIDATION.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add release automation and Homebrew formula</name>
  <files>.github/workflows/release-cli.yml, packaging/homebrew/marky.rb</files>
  <read_first>
    - package.json
    - scripts/verify-cli-package.mjs
    - .planning/phases/11-cli-distribution-and-homebrew-packaging/11-RESEARCH.md
  </read_first>
  <action>
Create `.github/workflows/release-cli.yml` with these exact responsibilities:
- trigger on version tags matching `v*`
- run `npm ci`
- run `npm run build`
- run `npm pack`
- upload the resulting `marky-*.tgz` as a GitHub release artifact

Create `packaging/homebrew/marky.rb` with these exact formula traits:
- `depends_on "node"`
- downloads the `marky-<version>.tgz` release artifact
- installs extracted contents under `libexec/"package"`
- writes a wrapper script named `marky` that executes `node #{libexec}/package/server/dist/cli.js "$@"`
- includes a `test do` block that runs `#{bin}/marky --no-open #{testpath}`

Do not point the formula at the source repo tarball; it must target the packaged artifact from Plan 11-03.
  </action>
  <verify>
    <automated>ruby -c /Users/romankarski/Documents/Documents_v2/Projects/Marky/packaging/homebrew/marky.rb && rg "npm pack|marky-\\*\\.tgz|depends_on \\\"node\\\"" /Users/romankarski/Documents/Documents_v2/Projects/Marky/.github/workflows/release-cli.yml /Users/romankarski/Documents/Documents_v2/Projects/Marky/packaging/homebrew/marky.rb</automated>
  </verify>
  <acceptance_criteria>
    - file `.github/workflows/release-cli.yml` exists
    - `.github/workflows/release-cli.yml` contains `npm pack`
    - `.github/workflows/release-cli.yml` contains `marky-`
    - file `packaging/homebrew/marky.rb` exists
    - `packaging/homebrew/marky.rb` contains `depends_on "node"`
    - `packaging/homebrew/marky.rb` contains `server/dist/cli.js`
    - `packaging/homebrew/marky.rb` contains `test do`
  </acceptance_criteria>
  <done>Release artifact automation and Homebrew formula both exist against the stable packed-artifact contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the real install flows</name>
  <files>README.md, scripts/verify-homebrew-install.mjs</files>
  <read_first>
    - packaging/homebrew/marky.rb
    - .github/workflows/release-cli.yml
    - scripts/verify-cli-package.mjs
  </read_first>
  <action>
Create `README.md` with a compact install section that documents:
- local repo use for development
- packed artifact install/use flow
- Homebrew install flow
- supported CLI flags: `marky`, `marky .`, `marky /path/to/folder`, `marky --port 4310`, `marky --no-open`

Create `scripts/verify-homebrew-install.mjs` as a checklist helper or smoke wrapper that prints the exact manual verification steps and validates preconditions such as release artifact URL presence.

Before the blocking checkpoint, run:
- `npm run build`
- `npm pack`
- `node scripts/verify-cli-package.mjs`

Then present the user with this human verification checklist:
1. Install the release artifact or tap the Homebrew formula
2. `cd` into a temp notes directory with at least one markdown file
3. Run `marky`
4. Confirm the browser opens unless `--no-open` is used
5. Confirm the app lists and opens the temp markdown file
6. Re-run with `marky --port 4310` and verify explicit-port behavior
7. Type `approved` only when the install flow works end to end
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm run build && npm pack && node scripts/verify-cli-package.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `brew install marky`
    - `README.md` contains `marky --no-open`
    - file `scripts/verify-homebrew-install.mjs` exists
    - user types `approved` after manual verification
  </acceptance_criteria>
  <done>The packaged CLI and Homebrew distribution flow are verified by a real install run.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `ruby -c /Users/romankarski/Documents/Documents_v2/Projects/Marky/packaging/homebrew/marky.rb`
2. `cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm run build && npm pack && node scripts/verify-cli-package.mjs`
3. Human verification checklist completed and user responds `approved`
</verification>

<success_criteria>
- Release workflow exists and packages the CLI artifact
- Homebrew formula exists against the packed artifact
- README documents install and CLI usage
- Human verification confirms the real install flow
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-distribution-and-homebrew-packaging/11-04-SUMMARY.md`
</output>

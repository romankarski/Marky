---
phase: 11-cli-distribution-and-homebrew-packaging
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - server/tests/cli/marky-cli.test.ts
  - server/tests/cli/port-and-open.test.ts
  - server/tests/cli/standalone-assets.test.ts
  - server/tests/cli/package-artifact.test.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - DIST-01

must_haves:
  truths:
    - "All four Wave 0 test files exist and fail for implementation reasons, not syntax errors"
    - "CLI tests lock the precedence ROOT_DIR > positional path > process.cwd()"
    - "Port tests lock explicit-port failure and default-port probing behavior"
    - "Package artifact tests lock the requirement that npm pack includes server/dist/cli.js, client/dist/index.html, and a root bin entry"
  artifacts:
    - path: "server/tests/cli/marky-cli.test.ts"
      provides: "CLI argument parsing and root-resolution contract"
    - path: "server/tests/cli/port-and-open.test.ts"
      provides: "Port collision and browser-open contract"
    - path: "server/tests/cli/standalone-assets.test.ts"
      provides: "Static asset serving contract for packaged mode"
    - path: "server/tests/cli/package-artifact.test.ts"
      provides: "npm pack artifact contract"
  key_links:
    - from: "server/tests/cli/marky-cli.test.ts"
      to: "server/src/cli.ts"
      via: "future CLI entrypoint import"
      pattern: "parseCliArgs"
    - from: "server/tests/cli/standalone-assets.test.ts"
      to: "server/src/start-server.ts"
      via: "future startup helper import"
      pattern: "startMarkyServer"
    - from: "server/tests/cli/package-artifact.test.ts"
      to: "package.json"
      via: "npm pack dry-run assertions"
      pattern: "\"bin\""
---

<objective>
Create Wave 0 RED tests for the entire Phase 11 CLI/distribution contract before implementation begins.

Purpose: Lock the behavior for CLI root resolution, port handling, browser-open gating, packaged static asset serving, and npm-pack artifact contents.
Output: Four RED test files in `server/tests/cli/` that fail because the implementation does not exist yet, not because the tests are malformed.
</objective>

<execution_context>
@/Users/romankarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-CONTEXT.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-RESEARCH.md
@.planning/phases/11-cli-distribution-and-homebrew-packaging/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CLI contract RED tests</name>
  <files>server/tests/cli/marky-cli.test.ts, server/tests/cli/port-and-open.test.ts</files>
  <read_first>
    - server/tests/api/files.test.ts
    - server/vitest.config.ts
    - server/src/index.ts
    - .planning/phases/11-cli-distribution-and-homebrew-packaging/11-RESEARCH.md
  </read_first>
  <action>
Create `server/tests/cli/marky-cli.test.ts` and `server/tests/cli/port-and-open.test.ts`.

`server/tests/cli/marky-cli.test.ts` must import future helpers from `../../src/cli.js` or `../../src/lib/cli/args.js` and assert these exact behaviors:
- `parseCliArgs([])` returns `{ rootArg: null, port: null, openBrowser: true }`
- `parseCliArgs(['.'])` returns `{ rootArg: '.', port: null, openBrowser: true }`
- `parseCliArgs(['/notes', '--port', '4310', '--no-open'])` returns `{ rootArg: '/notes', port: 4310, openBrowser: false }`
- `resolveRootDir({ envRootDir: '/env-root', rootArg: '/arg-root', cwd: '/cwd-root' })` returns `/env-root`
- `resolveRootDir({ envRootDir: '', rootArg: '/arg-root', cwd: '/cwd-root' })` returns `/arg-root`
- `resolveRootDir({ envRootDir: '', rootArg: null, cwd: '/cwd-root' })` returns `/cwd-root`

`server/tests/cli/port-and-open.test.ts` must assert these exact contracts:
- `resolvePort({ requestedPort: 4310, defaultPort: 4310 })` throws or rejects with `Port 4310 is already in use` when 4310 is occupied
- `resolvePort({ requestedPort: null, defaultPort: 4310 })` chooses the first free port in `4310..4320`
- `maybeOpenBrowser({ url: 'http://127.0.0.1:4310', openBrowser: false })` does not call the open helper
- `maybeOpenBrowser({ url: 'http://127.0.0.1:4310', openBrowser: true })` calls the open helper exactly once

Both files must begin with a header comment stating these are Wave 0 RED tests and that implementation does not exist yet.
Use Vitest APIs only; do not shell out from these tests.
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky/server && npx vitest run tests/cli/marky-cli.test.ts tests/cli/port-and-open.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - file `server/tests/cli/marky-cli.test.ts` exists
    - file `server/tests/cli/port-and-open.test.ts` exists
    - `server/tests/cli/marky-cli.test.ts` contains `ROOT_DIR > positional path > process.cwd()`
    - `server/tests/cli/marky-cli.test.ts` contains `--no-open`
    - `server/tests/cli/port-and-open.test.ts` contains `Port 4310 is already in use`
    - `server/tests/cli/port-and-open.test.ts` contains `4310..4320`
  </acceptance_criteria>
  <done>CLI helper tests exist and encode the exact Phase 11 runtime contract in RED state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create packaged-mode RED tests</name>
  <files>server/tests/cli/standalone-assets.test.ts, server/tests/cli/package-artifact.test.ts</files>
  <read_first>
    - server/tests/api/files.test.ts
    - package.json
    - server/src/index.ts
    - .planning/phases/11-cli-distribution-and-homebrew-packaging/11-RESEARCH.md
  </read_first>
  <action>
Create `server/tests/cli/standalone-assets.test.ts` and `server/tests/cli/package-artifact.test.ts`.

`server/tests/cli/standalone-assets.test.ts` must import a future `startMarkyServer` helper from `../../src/start-server.js` and lock these exact behaviors:
- when started with `staticDir` pointing at a temp folder containing `index.html`, GET `/` returns 200 and includes `<title>Marky</title>`
- API routes still work while static serving is enabled, using `buildApp`-style temp-root fixtures
- missing `staticDir` fails with a clear error mentioning `client/dist`

`server/tests/cli/package-artifact.test.ts` must use `execa`-style child-process logic via built-in `node:child_process` or `node:process` helpers to run `npm pack --json --dry-run` from repo root and assert the tarball file list contains:
- `server/dist/cli.js`
- `client/dist/index.html`
- `package.json`

The artifact test must also read root `package.json` and assert:
- `name === "marky"`
- `bin.marky === "server/dist/cli.js"`

These tests should currently fail because the package/build contract has not been implemented yet.
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky/server && npx vitest run tests/cli/standalone-assets.test.ts tests/cli/package-artifact.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - file `server/tests/cli/standalone-assets.test.ts` exists
    - file `server/tests/cli/package-artifact.test.ts` exists
    - `server/tests/cli/standalone-assets.test.ts` contains `client/dist`
    - `server/tests/cli/package-artifact.test.ts` contains `npm pack --json --dry-run`
    - `server/tests/cli/package-artifact.test.ts` contains `server/dist/cli.js`
    - `server/tests/cli/package-artifact.test.ts` contains `client/dist/index.html`
  </acceptance_criteria>
  <done>Packaged-mode and artifact-contract tests exist and fail until Phase 11 implementation lands.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `cd /Users/romankarski/Documents/Documents_v2/Projects/Marky/server && npx vitest run tests/cli/marky-cli.test.ts tests/cli/port-and-open.test.ts tests/cli/standalone-assets.test.ts tests/cli/package-artifact.test.ts --reporter=verbose`
2. Confirm failures are missing implementation / unmet assertions, not syntax errors
3. `rg "server/dist/cli.js|client/dist/index.html|ROOT_DIR > positional path > process.cwd\\(\\)" server/tests/cli`
</verification>

<success_criteria>
- Four CLI/distribution Wave 0 test files exist in `server/tests/cli/`
- Tests encode all six Phase 11 requirement IDs
- RED state is caused by missing implementation, not malformed tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-distribution-and-homebrew-packaging/11-01-SUMMARY.md`
</output>

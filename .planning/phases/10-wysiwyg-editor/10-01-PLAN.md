---
phase: 10-wysiwyg-editor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - client/package.json
  - server/package.json
  - client/src/__tests__/WysiwygEditor.test.tsx
  - client/src/__tests__/BubbleToolbar.test.tsx
  - client/src/__tests__/SlashCommandMenu.test.tsx
  - client/src/__tests__/EditorPane.test.tsx
  - client/src/__tests__/markdown-roundtrip.test.ts
  - client/src/extensions/__tests__/slash-commands.test.ts
  - server/tests/routes/upload-image.test.ts
autonomous: true
requirements:
  - WYSIWYG-01
  - WYSIWYG-02
  - WYSIWYG-03
  - WYSIWYG-04
  - WYSIWYG-05

must_haves:
  truths:
    - "All TipTap packages install without errors and resolve in node_modules"
    - "@fastify/multipart installs in server"
    - "Every Wave 0 test file exists and fails when run (RED state)"
  artifacts:
    - path: "client/package.json"
      provides: "TipTap dependencies"
      contains: "@tiptap/react"
    - path: "server/package.json"
      provides: "@fastify/multipart dependency"
      contains: "@fastify/multipart"
    - path: "client/src/__tests__/WysiwygEditor.test.tsx"
      provides: "WYSIWYG-01 and WYSIWYG-03 test stubs"
    - path: "client/src/__tests__/BubbleToolbar.test.tsx"
      provides: "WYSIWYG-04 test stubs"
    - path: "client/src/__tests__/SlashCommandMenu.test.tsx"
      provides: "WYSIWYG-02 test stubs"
    - path: "client/src/__tests__/EditorPane.test.tsx"
      provides: "WYSIWYG-05 test stubs"
    - path: "client/src/__tests__/markdown-roundtrip.test.ts"
      provides: "Round-trip fidelity test stubs"
    - path: "client/src/extensions/__tests__/slash-commands.test.ts"
      provides: "WYSIWYG-02 extension-level test stubs"
    - path: "server/tests/routes/upload-image.test.ts"
      provides: "WYSIWYG-03 server test stubs"
  key_links: []
---

<objective>
Install all TipTap dependencies and @fastify/multipart, then create Wave 0 test stubs for every Phase 10 requirement. All stubs must be in RED state (failing) to lock behavioral contracts before implementation begins.

Purpose: Establish the test scaffolding and dependency foundation for the entire WYSIWYG phase.
Output: Dependency-ready client/server + 7 failing test files covering WYSIWYG-01 through WYSIWYG-05.
</objective>

<execution_context>
@/Users/romankarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-wysiwyg-editor/10-CONTEXT.md
@.planning/phases/10-wysiwyg-editor/10-RESEARCH.md
@.planning/phases/10-wysiwyg-editor/10-UI-SPEC.md
@.planning/phases/10-wysiwyg-editor/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TipTap and multipart dependencies</name>
  <files>client/package.json, server/package.json</files>
  <read_first>
    - client/package.json
    - server/package.json
  </read_first>
  <action>
Install TipTap packages in client:
```bash
cd client && npm install @tiptap/react @tiptap/starter-kit @tiptap/markdown @tiptap/extension-link @tiptap/extension-image @tiptap/extension-table @tiptap/extension-table-row @tiptap/extension-table-cell @tiptap/extension-table-header @tiptap/extension-placeholder @tiptap/suggestion
```

Install multipart in server:
```bash
cd server && npm install @fastify/multipart
```

Verify both install cleanly with `npm ls @tiptap/react` and `npm ls @fastify/multipart`.

Create the extensions directory structure for Phase 10:
```bash
mkdir -p client/src/extensions/__tests__
```
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky/client && npm ls @tiptap/react @tiptap/starter-kit @tiptap/markdown && cd ../server && npm ls @fastify/multipart</automated>
  </verify>
  <acceptance_criteria>
    - client/package.json contains "@tiptap/react"
    - client/package.json contains "@tiptap/starter-kit"
    - client/package.json contains "@tiptap/markdown"
    - client/package.json contains "@tiptap/extension-link"
    - client/package.json contains "@tiptap/extension-image"
    - client/package.json contains "@tiptap/extension-table"
    - client/package.json contains "@tiptap/extension-table-row"
    - client/package.json contains "@tiptap/extension-table-cell"
    - client/package.json contains "@tiptap/extension-table-header"
    - client/package.json contains "@tiptap/extension-placeholder"
    - client/package.json contains "@tiptap/suggestion"
    - server/package.json contains "@fastify/multipart"
    - directory client/src/extensions/__tests__/ exists
  </acceptance_criteria>
  <done>All 11 TipTap packages installed in client, @fastify/multipart installed in server, extensions directory created</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs (RED state)</name>
  <files>
    client/src/__tests__/WysiwygEditor.test.tsx,
    client/src/__tests__/BubbleToolbar.test.tsx,
    client/src/__tests__/SlashCommandMenu.test.tsx,
    client/src/__tests__/EditorPane.test.tsx,
    client/src/__tests__/markdown-roundtrip.test.ts,
    client/src/extensions/__tests__/slash-commands.test.ts,
    server/tests/routes/upload-image.test.ts
  </files>
  <read_first>
    - client/src/__tests__/BacklinksPanel.test.tsx (reference for test pattern and imports)
    - client/src/__tests__/MarkdownPreview.test.tsx (reference for component test pattern)
    - server/tests/routes/templates.test.ts (reference for server route test pattern)
    - client/src/types/tabs.ts (Tab type definition)
    - client/src/hooks/useAutoSave.ts (auto-save signature)
  </read_first>
  <action>
Create 7 test files. Each file must import from the component/module path that WILL exist after implementation (imports will fail initially — that is the RED state). Every test uses `it.todo()` or `it('name', () => { expect(true).toBe(false); })` pattern to ensure RED.

**1. `client/src/__tests__/WysiwygEditor.test.tsx`** — WYSIWYG-01, WYSIWYG-03:
```typescript
import { describe, it, expect } from 'vitest';
// import { render, screen } from '@testing-library/react';
// import { WysiwygEditor } from '../components/WysiwygEditor';

describe('WysiwygEditor', () => {
  describe('WYSIWYG-01: click-to-edit', () => {
    it('renders an editable ProseMirror surface', () => {
      // WysiwygEditor renders <div class="ProseMirror" contenteditable="true">
      expect(true).toBe(false); // RED — component does not exist yet
    });

    it('applies prose prose-orange styling to editor content', () => {
      expect(true).toBe(false);
    });

    it('shows placeholder text when document is empty', () => {
      // placeholder: "Click anywhere to begin, or type / for commands"
      expect(true).toBe(false);
    });

    it('calls onChange with markdown string when content changes', () => {
      expect(true).toBe(false);
    });
  });

  describe('WYSIWYG-03: image insertion', () => {
    it('inserts image node after successful upload', () => {
      expect(true).toBe(false);
    });
  });
});
```

**2. `client/src/__tests__/BubbleToolbar.test.tsx`** — WYSIWYG-04:
```typescript
import { describe, it, expect } from 'vitest';

describe('BubbleToolbar', () => {
  it('renders Bold, Italic, Link, and Code buttons', () => {
    expect(true).toBe(false);
  });

  it('shows active state (orange-600) when mark is active', () => {
    expect(true).toBe(false);
  });

  it('toggles bold mark on button click', () => {
    expect(true).toBe(false);
  });

  it('toggles italic mark on button click', () => {
    expect(true).toBe(false);
  });
});
```

**3. `client/src/__tests__/SlashCommandMenu.test.tsx`** — WYSIWYG-02:
```typescript
import { describe, it, expect } from 'vitest';

describe('SlashCommandMenu', () => {
  it('renders 6 command items: h1, h2, h3, table, image, code', () => {
    expect(true).toBe(false);
  });

  it('filters items as query changes', () => {
    expect(true).toBe(false);
  });

  it('calls command handler when item is clicked', () => {
    expect(true).toBe(false);
  });

  it('highlights selected item with orange-100 background', () => {
    expect(true).toBe(false);
  });
});
```

**4. `client/src/extensions/__tests__/slash-commands.test.ts`** — WYSIWYG-02 extension:
```typescript
import { describe, it, expect } from 'vitest';

describe('slash-commands extension', () => {
  it('exports a TipTap Extension named slashCommands', () => {
    expect(true).toBe(false);
  });

  it('configures suggestion with char "/" and startOfLine true', () => {
    expect(true).toBe(false);
  });

  it('provides SLASH_ITEMS array with 6 entries', () => {
    expect(true).toBe(false);
  });
});
```

**5. `client/src/__tests__/EditorPane.test.tsx`** — WYSIWYG-05:
```typescript
import { describe, it, expect } from 'vitest';

describe('EditorPane — WYSIWYG mode', () => {
  describe('WYSIWYG-05: raw mode toggle', () => {
    it('renders WysiwygEditor by default (not split-pane)', () => {
      expect(true).toBe(false);
    });

    it('renders a </> toggle button in the toolbar', () => {
      expect(true).toBe(false);
    });

    it('switches to MarkdownEditor (CodeMirror) when toggle is clicked', () => {
      expect(true).toBe(false);
    });

    it('preserves content when toggling WYSIWYG -> raw', () => {
      expect(true).toBe(false);
    });

    it('preserves content when toggling raw -> WYSIWYG', () => {
      expect(true).toBe(false);
    });

    it('auto-save fires in WYSIWYG mode on content change', () => {
      expect(true).toBe(false);
    });

    it('auto-save fires in raw mode on content change', () => {
      expect(true).toBe(false);
    });
  });
});
```

**6. `client/src/__tests__/markdown-roundtrip.test.ts`** — ALL requirements:
```typescript
import { describe, it, expect } from 'vitest';

describe('Markdown round-trip fidelity', () => {
  it('preserves headings (h1, h2, h3) through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves bold and italic through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves links through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves inline code through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves fenced code blocks through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves GFM tables through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves images through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves bullet and numbered lists through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves blockquotes through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves horizontal rules through parse/serialize', () => {
    expect(true).toBe(false);
  });

  it('preserves YAML frontmatter through parse/serialize', () => {
    expect(true).toBe(false);
  });
});
```

**7. `server/tests/routes/upload-image.test.ts`** — WYSIWYG-03:
```typescript
import { describe, it, expect } from 'vitest';

describe('POST /api/upload-image', () => {
  it('saves uploaded file to rootDir/images/ directory', () => {
    expect(true).toBe(false);
  });

  it('returns JSON with root-relative path: { path: "/images/filename.png" }', () => {
    expect(true).toBe(false);
  });

  it('creates images/ directory if it does not exist', () => {
    expect(true).toBe(false);
  });

  it('returns 400 if no file is provided', () => {
    expect(true).toBe(false);
  });

  it('prepends timestamp to filename to avoid collisions', () => {
    expect(true).toBe(false);
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky/client && npx vitest run src/__tests__/WysiwygEditor.test.tsx src/__tests__/BubbleToolbar.test.tsx src/__tests__/SlashCommandMenu.test.tsx src/__tests__/EditorPane.test.tsx src/__tests__/markdown-roundtrip.test.ts src/extensions/__tests__/slash-commands.test.ts --reporter=verbose 2>&1 | tail -30; cd ../server && npx vitest run tests/routes/upload-image.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - client/src/__tests__/WysiwygEditor.test.tsx exists and contains "WYSIWYG-01" and "WYSIWYG-03"
    - client/src/__tests__/BubbleToolbar.test.tsx exists and contains "Bold, Italic, Link, and Code"
    - client/src/__tests__/SlashCommandMenu.test.tsx exists and contains "6 command items"
    - client/src/__tests__/EditorPane.test.tsx exists and contains "WYSIWYG-05"
    - client/src/__tests__/markdown-roundtrip.test.ts exists and contains "round-trip"
    - client/src/extensions/__tests__/slash-commands.test.ts exists and contains "slashCommands"
    - server/tests/routes/upload-image.test.ts exists and contains "POST /api/upload-image"
    - All test files run and FAIL (RED state — every test has expect(true).toBe(false))
  </acceptance_criteria>
  <done>7 test files created, all in RED state, covering WYSIWYG-01 through WYSIWYG-05 plus round-trip fidelity</done>
</task>

</tasks>

<verification>
- `npm ls @tiptap/react` resolves in client
- `npm ls @fastify/multipart` resolves in server
- All 7 test files exist and fail when run
- No existing tests broken by dependency installation
- `cd client && npx vitest run --reporter=verbose` shows all new tests RED, existing tests still pass
</verification>

<success_criteria>
- 11 TipTap packages in client dependencies
- @fastify/multipart in server dependencies
- 7 test stub files in RED state
- Existing test suite unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/10-wysiwyg-editor/10-01-SUMMARY.md`
</output>

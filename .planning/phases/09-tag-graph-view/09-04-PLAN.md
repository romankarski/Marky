---
phase: 09-tag-graph-view
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified: []
autonomous: false
requirements:
  - GRPH-01
  - GRPH-02
  - GRPH-03
  - GRPH-04

must_haves:
  truths:
    - "Automated graph route, persistence, and panel tests are green"
    - "The right rail exposes a persistent Graph tab, not a modal"
    - "Switching between Outline and Graph preserves the existing graph layout and viewport"
    - "The highlighted node tracks the currently focused file in split view as well as single-pane mode"
    - "Clicking nodes opens files in tabs during real app usage"
  artifacts:
    - path: "server/src/routes/graph.ts"
      provides: "Server graph payload endpoint used during verification"
    - path: "client/src/components/TagGraphPanel.tsx"
      provides: "Graph panel used during verification"
    - path: "client/src/App.tsx"
      provides: "Right-rail Graph tab wiring used during verification"
  key_links:
    - from: "client/src/components/TagGraphPanel.tsx"
      to: "server/src/routes/graph.ts"
      via: "runtime fetch during visual verification"
      pattern: "/api/graph/tags"
    - from: "client/src/App.tsx"
      to: "client/src/components/TagGraphPanel.tsx"
      via: "Outline | Graph tabset"
      pattern: "rightRailTab"
---

<objective>
Run the final automated regression commands for Phase 9 and complete the human verification pass for the new graph workflow.

Purpose: Close the phase with proof that the graph route, right-rail tabset, click-through behavior, and persistence work together in the real app.
Output: Green automated checks plus an explicit human approval checkpoint for the graph UX.
</objective>

<execution_context>
@/Users/romankarski/.codex/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tag-graph-view/09-VALIDATION.md
@.planning/phases/09-tag-graph-view/09-02-SUMMARY.md
@.planning/phases/09-tag-graph-view/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the Phase 9 automated verification suite</name>
  <files>server/tests/routes/graph.test.ts, client/src/__tests__/TagGraphPanel.test.tsx, client/src/hooks/__tests__/useTagGraphPersistence.test.ts, client/src/lib/__tests__/tagGraph.test.ts, client/src/App.tsx, client/src/components/TagGraphPanel.tsx, server/src/routes/graph.ts</files>
  <read_first>
    - .planning/phases/09-tag-graph-view/09-VALIDATION.md
    - client/src/components/TagGraphPanel.tsx
    - client/src/App.tsx
    - server/src/routes/graph.ts
    - server/tests/routes/graph.test.ts
  </read_first>
  <action>
Run the phase gate commands exactly as follows:

1. `npm test --workspace=server -- tests/routes/graph.test.ts`
2. `npm test --workspace=client`
3. `npx tsc --noEmit -p client/tsconfig.json`

Do not replace step 1 with the full server suite because `tests/routes/watch.test.ts` is a known unrelated failure in the current repo state. The client suite must be fully green because Phase 9 adds client utilities, component tests, and App wiring.
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm test --workspace=server -- tests/routes/graph.test.ts && npm test --workspace=client && npx tsc --noEmit -p client/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - the targeted server graph test passes
    - the full client test suite passes
    - client TypeScript compilation passes with `npx tsc --noEmit -p client/tsconfig.json`
    - no Phase 9 regression is waived as "manual only" when an automated check already exists
  </acceptance_criteria>
  <done>All automated Phase 9 checks are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the graph workflow</name>
  <files>client/src/App.tsx, client/src/components/TagGraphPanel.tsx, server/src/routes/graph.ts</files>
  <read_first>
    - .planning/phases/09-tag-graph-view/09-VALIDATION.md
    - client/src/App.tsx
    - client/src/components/TagGraphPanel.tsx
  </read_first>
  <action>Human verifies the completed graph experience in the running app after Task 1 is green.</action>
  <what-built>
    A persistent `Outline | Graph` tabset now lives in the right rail. The graph fetches `/api/graph/tags`, highlights the active file, remembers layout and viewport, and opens files when nodes are clicked.
  </what-built>
  <how-to-verify>
    1. Start the app with `npm run dev` from the repo root.
    2. Open any markdown file with tags, then switch the right rail from `Outline` to `Graph`.
    3. Verify the graph appears in the right rail, not as a modal or separate page.
    4. Pan and zoom the graph, switch back to `Outline`, then return to `Graph`. Confirm the node layout and viewport are preserved rather than restarting from the center.
    5. Refresh the browser while the graph tab is active and confirm the previous viewport is restored after the graph remounts.
    6. Click a node in the graph and confirm that file opens in a tab, the tree expands to it, and the recent-files list is updated through the existing open pattern.
    7. Enter split view, focus the left pane and then the right pane on different files, and confirm the highlighted node tracks the focused pane via `activeFocusedTab`.
    8. If your notes set is large enough, leave the graph open for several seconds and confirm the right rail remains responsive while panning and zooming.
  </how-to-verify>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm test --workspace=server -- tests/routes/graph.test.ts && npm test --workspace=client && npx tsc --noEmit -p client/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - the graph is accessible from the right rail and not from a modal
    - switching away from the graph and back preserves the previous layout and viewport
    - refreshing the page restores the saved graph viewport after the graph remounts
    - clicking a node opens the linked file in the app
    - the active node highlight follows the focused file in both single-pane and split-view states
    - the user explicitly approves the graph UX or returns concrete issues
  </acceptance_criteria>
  <done>User approved the complete graph workflow.</done>
  <resume-signal>Type "approved" or describe the observed issue.</resume-signal>
</task>

</tasks>

<verification>
Automated:
- `npm test --workspace=server -- tests/routes/graph.test.ts`
- `npm test --workspace=client`
- `npx tsc --noEmit -p client/tsconfig.json`

Manual:
- Verify the right-rail graph tab, active-node highlight, node click-through, and layout/viewport persistence in the running app.
</verification>

<success_criteria>
- Every Phase 9 requirement has been verified against either targeted automation or the final human checkpoint
- The graph route, UI, and persistence behave correctly together in the real app
- Phase 9 is ready for `$gsd-verify-work` with no missing execution steps
</success_criteria>

<output>
After completion, create `.planning/phases/09-tag-graph-view/09-04-SUMMARY.md`
</output>

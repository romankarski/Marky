---
phase: 09-tag-graph-view
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - client/src/lib/tagGraph.ts
  - client/src/hooks/useTagGraphPersistence.ts
  - client/src/components/TagGraphPanel.tsx
  - client/src/App.tsx
autonomous: true
requirements:
  - GRPH-02
  - GRPH-03
  - GRPH-04

must_haves:
  truths:
    - "The right rail exposes an `Outline | Graph` tabset below FileInfo and BacklinksPanel"
    - "The graph tab stays mounted while hidden so switching back does not restart the force simulation"
    - "Clicking a node opens the underlying file using the same `openTab + updateRecentFiles + expandFolder` pattern as other right-rail interactions"
    - "The active file highlight follows `activeFocusedTab?.path`, not the single-pane `activeTab`"
    - "Layout positions and viewport are restored from localStorage after tab switches and page reloads"
  artifacts:
    - path: "client/src/lib/tagGraph.ts"
      provides: "Client graph DTOs and snapshot merge helpers"
      exports: ["applyLayoutSnapshot", "captureNodePositions"]
    - path: "client/src/hooks/useTagGraphPersistence.ts"
      provides: "Graph snapshot and right-rail tab localStorage helpers"
      exports: ["loadTagGraphSnapshot", "saveTagGraphSnapshot", "loadRightRailTab", "saveRightRailTab"]
    - path: "client/src/components/TagGraphPanel.tsx"
      provides: "Graph UI, fetch lifecycle, active-node rendering, and persistence callbacks"
      exports: ["TagGraphPanel"]
    - path: "client/src/App.tsx"
      provides: "Persistent right-rail Outline | Graph tabset wired into existing right rail"
      contains: "rightRailTab"
  key_links:
    - from: "client/src/components/TagGraphPanel.tsx"
      to: "/api/graph/tags"
      via: "fetch on mount and refresh token changes"
      pattern: "fetch('/api/graph/tags')"
    - from: "client/src/components/TagGraphPanel.tsx"
      to: "client/src/hooks/useTagGraphPersistence.ts"
      via: "load/save layout snapshot and viewport"
      pattern: "saveTagGraphSnapshot"
    - from: "client/src/App.tsx"
      to: "client/src/components/TagGraphPanel.tsx"
      via: "persistent mounted panel hidden by tab state"
      pattern: "<TagGraphPanel"
---

<objective>
Implement the client graph panel, persistence helpers, and the new `Outline | Graph` right-rail tabset.

Purpose: Complete the user-facing graph experience with persistent layout/viewport state and active-file-aware navigation.
Output: `TagGraphPanel.tsx`, graph persistence utilities, and `App.tsx` right-rail wiring.
</objective>

<execution_context>
@/Users/romankarski/.codex/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tag-graph-view/09-RESEARCH.md
@.planning/phases/09-tag-graph-view/09-VALIDATION.md
@.planning/phases/06-tab-persistence-and-image-rendering/06-02-SUMMARY.md
@.planning/phases/08-backlinks-panel/08-03-SUMMARY.md
@.planning/phases/09-tag-graph-view/09-02-SUMMARY.md

<interfaces>
From client/src/App.tsx:
```tsx
const activeFocusedTab = splitMode
  ? (activePaneId === 'right' ? rightTab : leftTab)
  : activeTab;

<FileInfo ... />
<BacklinksPanel
  activeFilePath={activeFocusedTab?.path ?? null}
  onOpen={p => { openTab(p); updateRecentFiles(p); expandFolder(p); }}
/>
{tocContent ? (
  <TableOfContents content={tocContent} onHeadingClick={handleTocHeadingClick} />
) : (
  <div className="p-4 text-xs text-gray-300">No document open</div>
)}
```

From client/src/hooks/useTabPersistence.ts:
```ts
export function loadPersistedTabs(): PersistedTabEntry[];
export function saveTabState(tabs: PersistedTabEntry[], activeTabPath: string | null): void;
export function updateRecentFiles(path: string): void;
```

Server payload from Plan 02:
```ts
interface TagGraphNodePayload {
  id: string;
  path: string;
  label: string;
  tags: string[];
  tagCount: number;
}

interface TagGraphLinkPayload {
  source: string;
  target: string;
  sharedTags: string[];
  weight: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build graph DTO and persistence utilities</name>
  <files>client/src/lib/tagGraph.ts, client/src/hooks/useTagGraphPersistence.ts</files>
  <read_first>
    - client/src/hooks/useTabPersistence.ts
    - client/src/hooks/__tests__/useTagGraphPersistence.test.ts
    - client/src/lib/__tests__/tagGraph.test.ts
    - .planning/phases/06-tab-persistence-and-image-rendering/06-02-SUMMARY.md
    - .planning/phases/09-tag-graph-view/09-RESEARCH.md
  </read_first>
  <action>
Create `client/src/lib/tagGraph.ts` with the client-side graph contracts and pure helpers:
- export the node/link/data/viewport/snapshot types that mirror the server payload and add persisted `x`, `y`, `fx`, `fy` support on nodes
- export `applyLayoutSnapshot(nodes, snapshot)` to merge saved coordinates by `path`
- export `captureNodePositions(nodes)` to serialize only nodes whose coordinates are numeric

Create `client/src/hooks/useTagGraphPersistence.ts` following the plain-localStorage utility pattern used in `useTabPersistence.ts`.

Use these exact keys:
- `marky:tag-graph-layout` for `{ positions, viewport }`
- `marky:right-rail-tab` for `'outline' | 'graph'`

Export:
- `loadTagGraphSnapshot()`
- `saveTagGraphSnapshot(snapshot)`
- `loadRightRailTab()`
- `saveRightRailTab(tab)`

All functions must fail soft on corrupt JSON and absent localStorage entries. Keep this module free of React components so it remains easy to test and reuse from `App.tsx` plus `TagGraphPanel.tsx`.
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm test --workspace=client -- src/hooks/__tests__/useTagGraphPersistence.test.ts src/lib/__tests__/tagGraph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `client/src/lib/tagGraph.ts` exports the graph snapshot helper functions used by the tests
    - `client/src/hooks/useTagGraphPersistence.ts` uses the exact localStorage keys listed above
    - corrupt or missing localStorage values return safe defaults instead of throwing
    - the targeted persistence and pure utility tests pass GREEN
  </acceptance_criteria>
  <done>The client has tested graph data and persistence helpers ready for the UI layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TagGraphPanel and wire the right-rail tabset</name>
  <files>client/src/components/TagGraphPanel.tsx, client/src/App.tsx</files>
  <read_first>
    - client/src/App.tsx
    - client/src/components/BacklinksPanel.tsx
    - client/src/components/TableOfContents.tsx
    - client/src/__tests__/TagGraphPanel.test.tsx
    - client/src/lib/tagGraph.ts
    - client/src/hooks/useTagGraphPersistence.ts
    - .planning/phases/09-tag-graph-view/09-RESEARCH.md
    - .planning/phases/08-backlinks-panel/08-03-SUMMARY.md
  </read_first>
  <action>
Create `client/src/components/TagGraphPanel.tsx` and update `client/src/App.tsx`.

`TagGraphPanel.tsx` implementation requirements:
- Props:
  - `activeFilePath: string | null`
  - `isVisible: boolean`
  - `refreshToken: number`
  - `onOpen: (path: string) => void`
- Fetch `/api/graph/tags` on mount and whenever `refreshToken` changes.
- Merge saved node positions from `loadTagGraphSnapshot()` into the fetched payload before rendering.
- Use `react-force-graph-2d` and a `ref` so the component can call `pauseAnimation()` when hidden and `resumeAnimation()` plus `refresh()` when shown.
- Use a `ResizeObserver` on the container element and pass numeric `width` and `height` to `ForceGraph2D` so the canvas fits the right rail.
- After the graph data is loaded and the `ForceGraph2D` ref is ready, read `loadTagGraphSnapshot().viewport` and reapply the saved `{ x, y, k }` values to the graph instance so viewport state is restored on first render and page reload, not only after tab switches.
- Persist node positions on `onEngineStop` with `captureNodePositions(...)`.
- Persist viewport on `onZoomEnd`.
- Highlight the active file by comparing `node.path === activeFilePath` inside `nodeCanvasObject`; use the orange accent treatment from research and keep inactive nodes visually calmer.
- Clicking a node must call `onOpen(node.path)`.
- Render an inline loading state and an empty-state message when the payload has zero nodes.

`App.tsx` wiring requirements:
- Add `rightRailTab` state initialized from `loadRightRailTab()`.
- Add a `handleRightRailTabChange(tab)` helper that updates state and persists via `saveRightRailTab(tab)`.
- Replace the current TOC-only bottom section with a shared right-rail body:
  - top tab row with two buttons labeled `Outline` and `Graph`
  - one persistent container for `TableOfContents`
  - one persistent container for `TagGraphPanel`
- Keep `FileInfo` and `BacklinksPanel` above the new tabset unchanged.
- Use `activeFocusedTab?.path ?? null` for both the TOC context and the graph highlight source.
- Pass `onOpen={p => { openTab(p); updateRecentFiles(p); expandFolder(p); }}` into `TagGraphPanel`.
- Introduce a `graphRefreshVersion` state counter in `App.tsx`; increment it inside the same callback that currently triggers `refetchIndex` after tag edits so the graph refetches after FileInfo writes.

Do not conditionally mount/unmount the graph component when switching tabs. Hide it with classes/attributes while leaving it mounted.
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm test --workspace=client -- src/__tests__/TagGraphPanel.test.tsx src/hooks/__tests__/useTagGraphPersistence.test.ts src/lib/__tests__/tagGraph.test.ts && npx tsc --noEmit -p client/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `client/src/components/TagGraphPanel.tsx` exists and fetches `/api/graph/tags`
- clicking a node opens the file path through the existing right-rail open pattern
- the active node highlight is driven by `activeFocusedTab?.path`
- `TagGraphPanel.tsx` explicitly reapplies saved viewport coordinates from `loadTagGraphSnapshot().viewport` after mount
- switching between `Outline` and `Graph` leaves the graph mounted and preserves layout plus viewport
- `App.tsx` contains the persistent `Outline | Graph` tabset below FileInfo and BacklinksPanel
    - the targeted client tests pass GREEN and `npx tsc --noEmit -p client/tsconfig.json` succeeds
  </acceptance_criteria>
  <done>The client graph UI is live in the right rail with persistent layout, viewport, and active-file-aware navigation.</done>
</task>

</tasks>

<verification>
Run:
- `npm test --workspace=client -- src/__tests__/TagGraphPanel.test.tsx src/hooks/__tests__/useTagGraphPersistence.test.ts src/lib/__tests__/tagGraph.test.ts`
- `npx tsc --noEmit -p client/tsconfig.json`

Both must pass before moving to the phase verification plan.
</verification>

<success_criteria>
- GRPH-02, GRPH-03, and GRPH-04 are implemented in the client
- The right rail now uses a persistent `Outline | Graph` tabset instead of a TOC-only bottom section
- Graph layout/viewport state survives tab switches and page reloads
</success_criteria>

<output>
After completion, create `.planning/phases/09-tag-graph-view/09-03-SUMMARY.md`
</output>

---
phase: 09-tag-graph-view
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - client/package.json
  - package-lock.json
  - client/src/__tests__/TagGraphPanel.test.tsx
  - client/src/hooks/__tests__/useTagGraphPersistence.test.ts
  - client/src/lib/__tests__/tagGraph.test.ts
  - server/tests/routes/graph.test.ts
autonomous: true
requirements:
  - GRPH-01
  - GRPH-02
  - GRPH-03
  - GRPH-04

must_haves:
  truths:
    - "`react-force-graph-2d` is installed in the client workspace before implementation starts"
    - "All Phase 9 client and server test files exist before production code is written"
    - "Client graph tests mock `react-force-graph-2d` instead of asserting canvas pixels or simulation internals"
    - "Wave 0 tests fail in RED state because the graph modules and route are not implemented yet, not because the tests are malformed"
  artifacts:
    - path: "client/package.json"
      provides: "Graph dependency declaration"
      contains: "react-force-graph-2d"
    - path: "package-lock.json"
      provides: "Resolved workspace install for graph dependency"
      contains: "react-force-graph-2d"
    - path: "client/src/__tests__/TagGraphPanel.test.tsx"
      provides: "Client interaction and active-node-highlight test contract"
    - path: "client/src/hooks/__tests__/useTagGraphPersistence.test.ts"
      provides: "Layout and viewport persistence contract"
    - path: "client/src/lib/__tests__/tagGraph.test.ts"
      provides: "Pure graph snapshot merge/restore contract"
    - path: "server/tests/routes/graph.test.ts"
      provides: "Server graph payload contract"
  key_links:
    - from: "client/src/__tests__/TagGraphPanel.test.tsx"
      to: "client/src/components/TagGraphPanel.tsx"
      via: "direct import for future implementation"
      pattern: "from '../components/TagGraphPanel'"
    - from: "client/src/hooks/__tests__/useTagGraphPersistence.test.ts"
      to: "client/src/hooks/useTagGraphPersistence.ts"
      via: "direct import for future implementation"
      pattern: "from '../useTagGraphPersistence'"
    - from: "server/tests/routes/graph.test.ts"
      to: "/api/graph/tags"
      via: "Fastify inject route contract"
      pattern: "/api/graph/tags"
---

<objective>
Install the graph rendering dependency and create all Wave 0 Phase 9 test files before any graph implementation begins.

Purpose: Lock the graph route, persistence, and UI behavior contracts in RED state so Wave 1 implementation can go straight to GREEN.
Output: `react-force-graph-2d` installed plus four Phase 9 test files covering GRPH-01 through GRPH-04.
</objective>

<execution_context>
@/Users/romankarski/.codex/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tag-graph-view/09-RESEARCH.md
@.planning/phases/09-tag-graph-view/09-VALIDATION.md

<interfaces>
From client/src/App.tsx:
```tsx
const activeFocusedTab = splitMode
  ? (activePaneId === 'right' ? rightTab : leftTab)
  : activeTab;

<FileInfo ... />
<BacklinksPanel
  activeFilePath={activeFocusedTab?.path ?? null}
  onOpen={p => { openTab(p); updateRecentFiles(p); expandFolder(p); }}
/>
{tocContent ? (
  <TableOfContents content={tocContent} onHeadingClick={handleTocHeadingClick} />
) : (
  <div className="p-4 text-xs text-gray-300">No document open</div>
)}
```

From server/src/lib/search.ts:
```ts
export interface SearchDoc {
  id: string;
  name: string;
  path: string;
  text: string;
  tags: string[];
}
```

Reference test patterns:
- `client/src/__tests__/BacklinksPanel.test.tsx`
- `client/src/hooks/__tests__/useScrollPersist.test.ts`
- `server/tests/routes/search.test.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the client graph dependency</name>
  <files>client/package.json, package-lock.json</files>
  <read_first>
    - client/package.json
    - package.json
    - .planning/phases/09-tag-graph-view/09-RESEARCH.md
  </read_first>
  <action>
Run `npm install --workspace=client react-force-graph-2d` from the repo root so the client workspace gains the exact dependency recommended by research and the root `package-lock.json` records the resolved version.

Do not add alternative graph libraries. Do not modify server dependencies in this task.
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm ls --workspace=client react-force-graph-2d</automated>
  </verify>
  <acceptance_criteria>
    - client/package.json contains `react-force-graph-2d`
    - package-lock.json contains the resolved `react-force-graph-2d` entry
    - no other graph rendering library is introduced in this phase
  </acceptance_criteria>
  <done>`react-force-graph-2d` is installed and ready for the client graph implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create client Wave 0 graph and persistence tests</name>
  <files>
    client/src/__tests__/TagGraphPanel.test.tsx,
    client/src/hooks/__tests__/useTagGraphPersistence.test.ts,
    client/src/lib/__tests__/tagGraph.test.ts
  </files>
  <read_first>
    - client/src/__tests__/BacklinksPanel.test.tsx
    - client/src/hooks/__tests__/useScrollPersist.test.ts
    - client/src/test-setup.ts
    - client/src/App.tsx
    - .planning/phases/09-tag-graph-view/09-RESEARCH.md
    - .planning/phases/09-tag-graph-view/09-VALIDATION.md
  </read_first>
  <action>
Create three new test files that import the production modules that will exist after Wave 1.

`client/src/__tests__/TagGraphPanel.test.tsx`:
- Mock `react-force-graph-2d` with a lightweight test double that renders one button per node and captures `onNodeClick`, `onZoomEnd`, `onEngineStop`, `pauseAnimation`, `resumeAnimation`, and `nodeCanvasObject`.
- Assert GRPH-02 by verifying clicking the mocked node button calls `onOpen` with the node's `path`.
- Assert GRPH-03 by calling the captured `nodeCanvasObject` function with fake active and inactive nodes plus a stub canvas context, then checking the active node uses the highlighted fill/stroke treatment and larger radius.
- Assert the panel fetches `/api/graph/tags` and renders loading plus empty-state behavior without depending on canvas pixels.

`client/src/hooks/__tests__/useTagGraphPersistence.test.ts`:
- Import `loadTagGraphSnapshot`, `saveTagGraphSnapshot`, `loadRightRailTab`, and `saveRightRailTab` from `../useTagGraphPersistence`.
- Cover missing-localStorage, corrupt JSON, saved `positions`, saved `viewport`, and right-rail tab persistence using the exact localStorage keys that Wave 1 will implement.

`client/src/lib/__tests__/tagGraph.test.ts`:
- Import pure helpers from `../tagGraph`.
- Cover applying a saved snapshot by `path`, ignoring stale snapshot entries, and serializing node positions only when numeric coordinates are present.

Write the tests so they are syntactically valid and fail only because the production files do not exist yet.
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm test --workspace=client -- src/__tests__/TagGraphPanel.test.tsx src/hooks/__tests__/useTagGraphPersistence.test.ts src/lib/__tests__/tagGraph.test.ts 2>&1 | grep -E "(TagGraphPanel|useTagGraphPersistence|tagGraph|Cannot find module|Failed Suites)"</automated>
  </verify>
  <acceptance_criteria>
    - `client/src/__tests__/TagGraphPanel.test.tsx` exists and mocks `react-force-graph-2d`
    - the TagGraphPanel test file explicitly covers node click behavior and active-node highlighting
    - `client/src/hooks/__tests__/useTagGraphPersistence.test.ts` exists and names the snapshot plus right-rail tab behaviors
    - `client/src/lib/__tests__/tagGraph.test.ts` exists and exercises path-keyed snapshot merge behavior
    - the targeted client test command reaches RED state because the source files are absent, not because the tests crash before loading
  </acceptance_criteria>
  <done>All client graph and persistence contracts exist as readable RED tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the server graph route test contract</name>
  <files>server/tests/routes/graph.test.ts</files>
  <read_first>
    - server/tests/routes/search.test.ts
    - server/tests/routes/backlinks.test.ts
    - server/src/lib/search.ts
    - server/src/app.ts
    - .planning/phases/09-tag-graph-view/09-RESEARCH.md
    - .planning/phases/09-tag-graph-view/09-VALIDATION.md
  </read_first>
  <action>
Create `server/tests/routes/graph.test.ts` using the existing `buildApp` + `app.inject` pattern.

Build a temporary markdown fixture set that includes:
- two files sharing two tags so link weight aggregation can be asserted
- one file sharing exactly one tag with another file
- one untagged file that must still appear as an isolated node

Assert GRPH-01 with concrete route expectations:
- `GET /api/graph/tags` returns 200 with `{ nodes, links }`
- every markdown file becomes a node with `id`, `path`, `label`, `tags`, and `tagCount`
- untagged files are present in `nodes` even when they participate in no links
- shared-tag links include `source`, `target`, `sharedTags`, and numeric `weight`
- multiple shared tags between the same pair collapse into one link whose `weight` equals the number of shared tags

Because the route does not exist yet, the file should be valid but RED after creation.
  </action>
  <verify>
    <automated>cd /Users/romankarski/Documents/Documents_v2/Projects/Marky && npm test --workspace=server -- tests/routes/graph.test.ts 2>&1 | grep -E "(graph.test|GET /api/graph/tags|FAIL|404)"</automated>
  </verify>
  <acceptance_criteria>
    - `server/tests/routes/graph.test.ts` exists
    - the test fixture includes both tagged and untagged markdown files
    - the test names explicitly cover full node coverage and weighted shared-tag links
    - the targeted server test command reaches RED state because `/api/graph/tags` is not implemented yet
  </acceptance_criteria>
  <done>The server route contract for Phase 9 exists as a readable RED test file.</done>
</task>

</tasks>

<verification>
Wave 0 is complete when:
- `npm ls --workspace=client react-force-graph-2d` succeeds
- the three targeted client tests load and fail in RED state
- `server/tests/routes/graph.test.ts` loads and fails in RED state
</verification>

<success_criteria>
- Phase 9 has explicit Wave 0 coverage for graph rendering, persistence, and server payload behavior
- The graph library is installed before any UI implementation begins
- No Wave 1 task needs to invent its own tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-tag-graph-view/09-01-SUMMARY.md`
</output>

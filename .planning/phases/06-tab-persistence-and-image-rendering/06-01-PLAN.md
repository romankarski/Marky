---
phase: 06-tab-persistence-and-image-rendering
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - client/src/hooks/__tests__/useTabPersistence.test.ts
  - client/src/hooks/__tests__/useScrollPersist.test.ts
  - client/src/__tests__/MarkdownPreview.test.tsx
  - server/tests/routes/images.test.ts
autonomous: true
requirements:
  - PRST-01
  - PRST-02
  - PRST-03
  - IMG-01
  - IMG-02

must_haves:
  truths:
    - "All four test files exist with failing (RED) tests"
    - "useTabPersistence tests assert tab restore and recent-files behavior against mocked localStorage"
    - "useScrollPersist tests assert debounced save and useLayoutEffect restore against mocked DOM refs"
    - "MarkdownPreview test asserts img src attribute is a /api/image proxy URL with correct encoded path"
    - "images.test.ts asserts 200 with correct Content-Type for valid in-scope paths and 403 for traversal attempts"
    - "npm run test in both workspaces exits with test failures — not compilation errors"
  artifacts:
    - path: "client/src/hooks/__tests__/useTabPersistence.test.ts"
      provides: "RED stubs for PRST-01 and PRST-02"
    - path: "client/src/hooks/__tests__/useScrollPersist.test.ts"
      provides: "RED stubs for PRST-03"
    - path: "client/src/__tests__/MarkdownPreview.test.tsx"
      provides: "RED stub for IMG-01 client-side path resolution"
    - path: "server/tests/routes/images.test.ts"
      provides: "RED integration stubs for IMG-02"
  key_links:
    - from: "useTabPersistence.test.ts"
      to: "client/src/hooks/useTabPersistence.ts"
      via: "import (file does not exist yet — test imports will fail until Wave 1)"
      pattern: "from.*useTabPersistence"
    - from: "useScrollPersist.test.ts"
      to: "client/src/hooks/useScrollPersist.ts"
      via: "import (file does not exist yet)"
      pattern: "from.*useScrollPersist"
---

<objective>
Create all four Wave 0 failing test files so that every Phase 6 requirement has automated coverage before implementation begins. Tests must be RED (fail due to missing implementation, not compilation errors).

Purpose: Nyquist rule compliance — every auto task in Wave 1 has a pre-existing automated test to go GREEN against.
Output: Four test files with failing stubs.
</objective>

<execution_context>
@/Users/romankarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tab-persistence-and-image-rendering/06-CONTEXT.md
@.planning/phases/06-tab-persistence-and-image-rendering/06-VALIDATION.md

<interfaces>
<!-- Existing test patterns to match. From codebase inspection. -->

From client/src/hooks/__tests__/useAutoSave.test.ts (pattern reference):
```typescript
import { renderHook, act } from '@testing-library/react';
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
// Pattern: vi.useFakeTimers() for debounce tests, vi.stubGlobal for localStorage
```

From client/src/__tests__/useTabs.test.ts (existing Tab type shape):
```typescript
// Tab type has: id, path, label, content, loading, dirty, editMode, deleted
// useTabs returns: { tabs, activeTabId, dispatch }
```

From server/tests/api/files.test.ts (server test pattern):
```typescript
import Fastify from 'fastify';
import { buildApp } from '../../src/app.js';
// Pattern: build app, inject requests, assert status and body
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Client test stubs — useTabPersistence and useScrollPersist</name>
  <files>client/src/hooks/__tests__/useTabPersistence.test.ts, client/src/hooks/__tests__/useScrollPersist.test.ts</files>
  <behavior>
    useTabPersistence.test.ts:
    - Test: loadPersistedTabs returns empty array when localStorage is empty
    - Test: loadPersistedTabs returns parsed tabs array when localStorage has valid JSON
    - Test: loadPersistedTabs returns empty array when localStorage has corrupt JSON
    - Test: saveTabState writes serialized { tabs: [{path, label}], activeTabPath } to localStorage key 'marky:tabs'
    - Test: updateRecentFiles prepends new path, deduplicates, caps at 5 entries
    - Test: getRecentFiles returns empty array when localStorage key 'marky:recent' is absent

    useScrollPersist.test.ts:
    - Test: scroll position is saved to localStorage key 'marky:scroll' after 200ms debounce
    - Test: scroll position is NOT saved before 200ms debounce fires
    - Test: getScrollPosition returns undefined when key absent
    - Test: getScrollPosition returns saved number when key present

    All tests import from hooks that do NOT yet exist — tests will fail with module-not-found or function-not-found errors (RED state is acceptable as long as the test runner sees them as failures, not compile panics).
  </behavior>
  <action>
    Create useTabPersistence.test.ts using renderHook from @testing-library/react (already installed at ^16.3.2). Mock localStorage using vi.stubGlobal('localStorage', ...) with a Map-backed mock. Use vi.useFakeTimers() for debounce assertions in scroll tests.

    Import paths: `import { loadPersistedTabs, saveTabState, updateRecentFiles, getRecentFiles } from '../useTabPersistence'` — these exports do not exist yet; that is intentional.

    Create useScrollPersist.test.ts importing `import { saveScrollPosition, getScrollPosition } from '../useScrollPersist'` — does not exist yet.

    Both files must contain at minimum 3 describe blocks and 6 total test cases (3 per file minimum) that will fail because the source files are missing.
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky && npm run test --workspace=client -- --reporter=verbose 2>&1 | grep -E "(FAIL|Cannot find module|useTabPersistence|useScrollPersist)"</automated>
  </verify>
  <done>Both test files exist. Running the client test suite shows failures attributable to missing source modules (not TypeScript compilation failures that prevent the test runner from loading at all). Test descriptions are readable in output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client test stub — MarkdownPreview image path resolution (IMG-01)</name>
  <files>client/src/__tests__/MarkdownPreview.test.tsx</files>
  <behavior>
    MarkdownPreview.test.tsx (extend or create — check if file exists first):
    - Test: img with relative path `./screenshot.png` and filePath `docs/notes.md` renders with src `/api/image?path=docs%2Fscreenshot.png`
    - Test: img with relative path `../assets/photo.jpg` and filePath `docs/notes.md` renders with src containing `/api/image?path=` and `assets%2Fphoto.jpg`
    - Test: img with absolute path `/Users/romankarski/notes/img.png` renders with src `/api/image?path=%2FUsers%2Fromankarski%2Fnotes%2Fimg.png`
    - Test: img with remote URL `https://example.com/img.png` renders with src `https://example.com/img.png` (passthrough — no proxy)
    - Test: MarkdownPreview renders without error when filePath prop is provided (smoke test)
  </behavior>
  <action>
    Check if client/src/__tests__/MarkdownPreview.test.tsx already exists. If it exists, read it first and add the new img tests to the existing file without removing existing tests.

    Use @testing-library/react render + screen.getByRole('img') or container.querySelector('img') to assert src attributes.

    The component will need a new `filePath` prop that does not exist yet — tests will fail when the component is rendered without the prop resolving correctly (RED state).

    Import: `import MarkdownPreview from '../components/MarkdownPreview'`
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky && npm run test --workspace=client -- --reporter=verbose 2>&1 | grep -E "(FAIL|PASS|MarkdownPreview)"</automated>
  </verify>
  <done>MarkdownPreview test file exists. At least the img-related test cases exist and are RED (failing because filePath prop behavior not yet implemented or prop absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Server test stub — image proxy route (IMG-02)</name>
  <files>server/tests/routes/images.test.ts</files>
  <behavior>
    images.test.ts:
    - Test: GET /api/image?path=<valid-png-path> returns 200 with Content-Type image/png
    - Test: GET /api/image?path=<valid-svg-path> returns 200 with Content-Type image/svg+xml
    - Test: GET /api/image?path=../../../etc/passwd (traversal) returns 403
    - Test: GET /api/image?path=<nonexistent> returns 404
    - Test: GET /api/image?path=<valid-path-outside-rootDir> returns 403
    - Test: GET /api/image with no path query param returns 400 or 403

    Tests import buildApp and inject requests (same pattern as server/tests/api/files.test.ts). The route /api/image does not exist yet — all tests will fail with 404 from Fastify (or route not found — RED state).
  </behavior>
  <action>
    Read server/tests/api/files.test.ts first to copy the exact buildApp + inject pattern. Mirror that structure.

    For test fixtures: use a small fixture approach — create a temporary test PNG as a Buffer (1x1 pixel PNG bytes) written to a temp dir within test setup. Use the same rootDir as the buildApp fixture so path security works.

    Alternatively (simpler): stub fs.readFile in the server test using vi.mock — check which approach the existing files.test.ts uses and follow it.

    Import: the route file `server/src/routes/images.ts` does not exist yet — the test will fail because the route returns 404 from Fastify (route not registered).
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky && npm run test --workspace=server 2>&1 | grep -E "(FAIL|PASS|images)"</automated>
  </verify>
  <done>server/tests/routes/images.test.ts exists. Running the server test suite shows this test file failing (routes return 404 because the implementation is not yet registered). All test descriptions are readable.</done>
</task>

</tasks>

<verification>
All four test files exist on disk. Running the full test suite shows failures for the new tests — not TypeScript panics or import resolution failures that prevent the suite from loading. Existing passing tests from Phase 5 remain GREEN.
</verification>

<success_criteria>
- client/src/hooks/__tests__/useTabPersistence.test.ts exists with 6+ test cases
- client/src/hooks/__tests__/useScrollPersist.test.ts exists with 4+ test cases
- client/src/__tests__/MarkdownPreview.test.tsx has img proxy URL test cases
- server/tests/routes/images.test.ts exists with 6+ test cases
- `npm run test --workspace=client && npm run test --workspace=server` runs without crashing the test runner (test failures are expected, test runner crashes are not)
- All existing Phase 5 tests remain GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/06-tab-persistence-and-image-rendering/06-01-SUMMARY.md`
</output>

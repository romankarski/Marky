---
phase: 06-tab-persistence-and-image-rendering
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified: []
autonomous: false
requirements:
  - PRST-01
  - PRST-02
  - PRST-03
  - IMG-01
  - IMG-02

must_haves:
  truths:
    - "All previously open tabs reopen after page reload"
    - "Scroll position restored after reload (verified on a long document)"
    - "Welcome screen shows recent files list"
    - "Local images render in preview"
    - "All automated tests are GREEN"
  artifacts:
    - path: "client/src/hooks/useTabPersistence.ts"
      provides: "Implemented and tested"
    - path: "client/src/hooks/useScrollPersist.ts"
      provides: "Implemented and tested"
    - path: "server/src/routes/images.ts"
      provides: "Implemented and tested"
    - path: "client/src/components/MarkdownPreview.tsx"
      provides: "img proxy override implemented"
  key_links:
    - from: "Browser localStorage"
      to: "App.tsx useTabs state"
      via: "useTabPersistence read on mount, write on change"
      pattern: "marky:tabs"
    - from: "Browser localStorage"
      to: "preview scroll container"
      via: "useScrollPersist read in useLayoutEffect, write on scroll"
      pattern: "marky:scroll"
---

<objective>
Verify Phase 6 functionality end-to-end in the running application. Confirm tab persistence, scroll restore, recent files, and local image rendering all work correctly in the browser.

Purpose: Human verification of the complete Phase 6 user experience before marking the phase done.
Output: Confirmed Phase 6 SUCCESS or a list of issues to fix.
</objective>

<execution_context>
@/Users/romankarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-tab-persistence-and-image-rendering/06-CONTEXT.md
@.planning/phases/06-tab-persistence-and-image-rendering/06-02-SUMMARY.md
@.planning/phases/06-tab-persistence-and-image-rendering/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm GREEN</name>
  <files></files>
  <action>
    Run the complete test suite for both workspaces:

    ```
    npm run test --workspace=client && npm run test --workspace=server
    ```

    If any test is RED, fix it before proceeding to the human verification checkpoint. Do not proceed with a failing suite.
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky && npm run test --workspace=client && npm run test --workspace=server</automated>
  </verify>
  <done>Both workspaces exit 0. All tests GREEN — including the Phase 6 stubs from Plan 01 that are now implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 6 complete implementation:
    - Tab persistence: open tabs survive page reload (PRST-01)
    - Recent files: welcome screen shows up to 5 recent files with filename + folder path (PRST-02)
    - Scroll restore: each tab's scroll position restored on reload and when switching tabs (PRST-03)
    - Image proxy: local images render inline in MarkdownPreview (IMG-01, IMG-02)
  </what-built>
  <how-to-verify>
    Start the app: `npm run dev` from the Marky root.

    **Test PRST-01 (tab persistence):**
    1. Open 3 different markdown files in tabs
    2. Switch to the second tab (not the first)
    3. Refresh the page (Cmd+R)
    4. Verify: all 3 tabs reopen, and the second tab is focused (not the first)

    **Test PRST-02 (recent files):**
    1. Open several different markdown files by clicking them in the file tree
    2. Close all tabs (click X on each tab) until the welcome screen appears
    3. Verify: welcome screen shows a "Recent Files" section with up to 5 files
    4. Each entry should show: filename prominently (e.g., "decisions.md") with folder path below (e.g., "knowledge/current")
    5. Click one of the recent file entries — verify it opens as a tab

    **Test PRST-03 (scroll restore):**
    1. Open a long markdown file (find one with 100+ lines in your knowledge base)
    2. Scroll down to approximately the middle of the document
    3. Refresh the page (Cmd+R)
    4. Verify: the tab reopens and the preview is scrolled to the same position (not back to the top)
    5. Also test tab-switch scroll: open two long files, scroll each to a different position, switch between tabs — verify each tab remembers its scroll position

    **Test IMG-01 (relative image paths):**
    1. Find or create a markdown file that references a local image with a relative path: `![test](./screenshot.png)` or `![test](../assets/photo.jpg)`
    2. Place an actual image file at that relative path
    3. Open the markdown file in the app
    4. Verify: the image renders inline in the preview (not a placeholder or broken image icon)

    **Test IMG-02 (absolute image paths):**
    1. Find or create a markdown file that references a local image with an absolute path: `![test](/Users/romankarski/notes/img.png)` (use a real path on your system)
    2. Verify: the image renders inline in the preview
    3. Also verify: an image path OUTSIDE ROOT_DIR shows a broken image icon (not an app error)
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 5 success criteria from the phase goal are observable in the running application.
</verification>

<success_criteria>
- PRST-01: After page refresh, all open tabs reopen with correct files and active tab focused
- PRST-02: Welcome screen shows recent files list (filename + folder path, up to 5 entries, clickable)
- PRST-03: Scroll position restored after reload and when switching tabs
- IMG-01: Relative-path images (./img.png, ../path/img.png) render inline in preview
- IMG-02: Absolute-path images within ROOT_DIR render inline; out-of-scope paths show broken image icon
- All automated tests GREEN in both workspaces
</success_criteria>

<output>
After completion, create `.planning/phases/06-tab-persistence-and-image-rendering/06-04-SUMMARY.md`
</output>

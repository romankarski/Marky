---
phase: 07-file-templates
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - client/src/lib/builtInTemplates.ts
  - client/src/lib/templateTokens.ts
  - client/src/components/FolderPickerModal.tsx
  - client/src/components/FileTree.tsx
autonomous: true
requirements:
  - TMPL-01
  - TMPL-03

must_haves:
  truths:
    - "Clicking '+ New' shows a template picker step (step 1) listing Blank + 3 built-in templates before the filename/folder step"
    - "Selecting 'Daily Note' template creates a file pre-filled with YAML frontmatter, today's date in {{date}} positions, and {{title}} resolved to the filename without .md"
    - "Selecting 'Blank' skips template content and creates an empty file (same behavior as before Phase 7)"
    - "Token substitution handles .md extension strip: filename 'my-meeting.md' → title token 'my-meeting'"
  artifacts:
    - path: "client/src/lib/builtInTemplates.ts"
      provides: "BUILT_IN_TEMPLATES constant — 3 TemplateEntry objects"
      exports: ["TemplateEntry", "BUILT_IN_TEMPLATES"]
    - path: "client/src/lib/templateTokens.ts"
      provides: "applyTokens pure function"
      exports: ["applyTokens"]
    - path: "client/src/components/FolderPickerModal.tsx"
      provides: "Two-step modal: step=template then step=location"
      contains: "step state, handleTemplateSelect, template grid, onConfirm(filePath, fileName, content)"
    - path: "client/src/components/FileTree.tsx"
      provides: "Updated handleCreate accepting content parameter"
      contains: "handleCreate(filePath, content) passing content to POST body"
  key_links:
    - from: "client/src/components/FileTree.tsx"
      to: "client/src/components/FolderPickerModal.tsx"
      via: "onConfirm callback with updated (filePath, fileName, content) signature"
    - from: "client/src/components/FolderPickerModal.tsx"
      to: "client/src/lib/builtInTemplates.ts"
      via: "import BUILT_IN_TEMPLATES for rendering step 1 list"
    - from: "client/src/components/FolderPickerModal.tsx"
      to: "client/src/lib/templateTokens.ts"
      via: "applyTokens called in handleConfirm before invoking onConfirm"
---

<objective>
Implement the client-side template foundation: built-in template constants, token substitution function, and the two-step FolderPickerModal flow, plus the matching FileTree wiring.

Purpose: Turns the RED Wave 0 tests for TMPL-01 and TMPL-03 GREEN. After this plan, users can create files from built-in templates with tokens substituted at creation time.
Output: 4 modified/created client files. Template picker step appears on "+ New". Built-in templates create pre-filled files.
</objective>

<execution_context>
@/Users/romankarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-file-templates/07-RESEARCH.md
@.planning/phases/07-file-templates/07-01-SUMMARY.md

<interfaces>
<!-- Exact current signatures to update — extracted from source. -->

From client/src/components/FolderPickerModal.tsx (current):
```typescript
interface Props {
  defaultFolder?: string;
  onConfirm: (filePath: string, fileName: string) => void;  // CHANGES in this plan
  onCancel: () => void;
}

// Current handleConfirm (single step):
const handleConfirm = () => {
  const name = fileName.trim();
  if (!name) return;
  const safeName = name.endsWith('.md') ? name : `${name}.md`;
  const fullPath = selectedFolder ? `${selectedFolder}/${safeName}` : safeName;
  onConfirm(fullPath, safeName);
};
```

From client/src/components/FileTree.tsx (current):
```typescript
// handleCreate — needs content parameter added:
const handleCreate = async (filePath: string) => {
  const res = await fetch(`/api/files/${filePath}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ content: '' }),  // CHANGES to use content param
  });
  // ...
};

// onConfirm wiring in JSX (current):
onConfirm={(filePath) => handleCreate(filePath)}
// CHANGES to:
// onConfirm={(filePath, _fileName, content) => handleCreate(filePath, content)}
```

From client/src/lib/export.ts (existing pattern for client lib files):
```typescript
// Existing lib file pattern — plain TypeScript, no React, exported functions
export function applyTokens(...) { ... }
```

From server/src/lib/fsTree.ts (confirmed dot-dir filter):
```typescript
// Line 10: if (entry.name.startsWith('.')) continue;
// CONFIRMED: .marky/ directory will NOT appear in the file tree. No additional filter needed.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create builtInTemplates.ts and templateTokens.ts</name>
  <files>client/src/lib/builtInTemplates.ts, client/src/lib/templateTokens.ts</files>
  <behavior>
    builtInTemplates.ts:
    - Exports TemplateEntry interface: { id: string; label: string; content: string }
    - Exports BUILT_IN_TEMPLATES array with exactly 3 entries:
      1. id='daily-note', label='Daily Note', content has YAML frontmatter with date/tags fields, heading '# {{date}}', sections "## Today" and "## Notes"
      2. id='meeting-note', label='Meeting Note', content has YAML frontmatter, heading '# Meeting: {{title}}', sections Attendees/Agenda/Notes/Action Items
      3. id='decision-record', label='Decision Record', content has YAML frontmatter, heading '# Decision: {{title}}', sections Context/Decision/Consequences

    templateTokens.ts:
    - Exports applyTokens(content: string, vars: { title: string; date: string }): string
    - Replaces {{date}} globally with vars.date
    - Replaces {{title}} globally with vars.title after stripping .md extension from vars.title
    - Uses global regex /\{\{date\}\}/g and /\{\{title\}\}/g
  </behavior>
  <action>
    Create `client/src/lib/builtInTemplates.ts` with the TemplateEntry interface and BUILT_IN_TEMPLATES constant. Template content strings use raw `{{date}}` and `{{title}}` tokens — substitution happens at creation time in FolderPickerModal, not here.

    Daily note content (exact):
    ```
    ---\ndate: {{date}}\ntags: [daily]\n---\n\n# {{date}}\n\n## Today\n\n- \n\n## Notes\n\n
    ```

    Meeting note content (exact):
    ```
    ---\ndate: {{date}}\ntags: [meeting]\n---\n\n# Meeting: {{title}}\n\n**Date:** {{date}}\n\n## Attendees\n\n- \n\n## Agenda\n\n1. \n\n## Notes\n\n## Action Items\n\n- [ ] \n
    ```

    Decision record content (exact):
    ```
    ---\ndate: {{date}}\ntags: [decision]\n---\n\n# Decision: {{title}}\n\n**Date:** {{date}}\n**Status:** Proposed\n\n## Context\n\n## Decision\n\n## Consequences\n\n
    ```

    Create `client/src/lib/templateTokens.ts` with the applyTokens function. Strip .md from title before substitution: `vars.title.replace(/\.md$/, '')`.
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky/client && npm test -- --run 2>&1 | grep -E "(templateTokens|builtInTemplates)" | head -10</automated>
  </verify>
  <done>Both lib test files (from Wave 0) now pass GREEN. No other client tests regress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend FolderPickerModal to two-step flow + update FileTree wiring</name>
  <files>client/src/components/FolderPickerModal.tsx, client/src/components/FileTree.tsx</files>
  <behavior>
    FolderPickerModal behavior:
    - On open: shows step 1 "template" with 4 options: Blank + 3 built-in templates
    - Each template shown as a clickable card/button with its label
    - Blank option is always first, labeled "Blank" (empty content)
    - Clicking any template option stores content in state and advances to step 2 (location)
    - Step 2 is the existing filename/folder UI — unchanged behavior
    - onConfirm signature changes to (filePath: string, fileName: string, content: string)
    - applyTokens called in handleConfirm with { title: fileName (before .md appended), date: YYYY-MM-DD today }
    - Modal header title stays "New File" across both steps
    - Back button in step 2 returns to step 1 (optional: include if clean, skip if it adds complexity)

    FileTree behavior:
    - handleCreate signature: (filePath: string, content: string = '')
    - POST body: { content } instead of { content: '' }
    - onConfirm wiring updated to pass content as third argument
  </behavior>
  <action>
    Modify `client/src/components/FolderPickerModal.tsx`:

    1. Add imports: `import { BUILT_IN_TEMPLATES } from '../lib/builtInTemplates';` and `import { applyTokens } from '../lib/templateTokens';`
    2. Add step state: `const [step, setStep] = useState<'template' | 'location'>('template');`
    3. Add selectedContent state: `const [selectedContent, setSelectedContent] = useState('');`
    4. Add handleTemplateSelect: `(content: string) => { setSelectedContent(content); setStep('location'); }`
    5. Update Props interface: `onConfirm: (filePath: string, fileName: string, content: string) => void`
    6. Update handleConfirm to call applyTokens before invoking onConfirm:
       - title = safeName without .md extension (safeName already has .md, so strip it)
       - date = new Date().toISOString().slice(0, 10)
       - content = applyTokens(selectedContent, { title, date })
       - Call onConfirm(fullPath, safeName, content)
    7. Render step 1 when step === 'template': a list/grid of buttons — first "Blank" (white bg), then BUILT_IN_TEMPLATES mapped to buttons showing entry.label. Style consistently with existing modal: text-sm, hover:bg-orange-50, border, rounded.
    8. Render existing location UI only when step === 'location'. Keep all existing location logic intact.
    9. The `autoFocus` on the filename input in step 2 is preserved.

    Template step layout (step 1) should use the same modal shell (fixed inset, bg-white, rounded-lg, w-96):
    - Header: "New File" + ✕ cancel button (same as before)
    - Body: padding px-4 py-3, label "Choose a template", then a vertical list of 4 clickable items
    - Each item: full-width button, py-2 px-3, text-sm, text-left, hover:bg-orange-50, border-b border-gray-100
    - Blank item has a small gray description "Start with an empty file"
    - Built-in items show their label and a small description derived from content structure

    Modify `client/src/components/FileTree.tsx`:
    1. Update handleCreate signature: `async (filePath: string, content: string = '')`
    2. Change `body: JSON.stringify({ content: '' })` to `body: JSON.stringify({ content })`
    3. Update onConfirm wiring in JSX:
       `onConfirm={(filePath, _fileName, content) => handleCreate(filePath, content)}`

    Anti-patterns to avoid (from research):
    - Do NOT pass `undefined` as content — the default parameter `content = ''` ensures blank still works
    - Do NOT apply tokens twice — applyTokens is called exactly once in handleConfirm
    - Do NOT include .md in the title token — derive title from fileName before .md is appended (i.e. use `fileName.trim()` before the `.md` suffix is added, OR strip it after: `safeName.replace(/\.md$/, '')`)
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky/client && npm test -- --run 2>&1 | grep -E "(PASS|FAIL|passed|failed)" | tail -5</automated>
  </verify>
  <done>TemplatePickerModal.test.tsx Wave 0 stubs pass GREEN. All previously passing client tests still pass. TypeScript compiles without errors (tsc --noEmit passes or no type errors on the modified files).</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `cd /Users/romankarski/projects/portal-hub/Marky/client && npm test -- --run` — all tests pass including the 3 Wave 0 stub files
2. `cd /Users/romankarski/projects/portal-hub/Marky/client && npx tsc --noEmit` — no TypeScript errors in FolderPickerModal.tsx or FileTree.tsx
3. BUILT_IN_TEMPLATES exports 3 entries: `grep -c "id:" client/src/lib/builtInTemplates.ts` returns 3
4. applyTokens strips .md from title: confirmed by Wave 0 test passing
</verification>

<success_criteria>
- client/src/lib/builtInTemplates.ts exports TemplateEntry interface and BUILT_IN_TEMPLATES with 3 entries
- client/src/lib/templateTokens.ts exports applyTokens that substitutes {{date}} and {{title}}, strips .md from title
- FolderPickerModal renders a template selection step on open (step 1) before the filename step (step 2)
- FileTree.handleCreate passes content to POST body
- All Wave 0 client tests for TMPL-01 and TMPL-03 pass GREEN
- No previously-passing client tests regress
</success_criteria>

<output>
After completion, create `.planning/phases/07-file-templates/07-02-SUMMARY.md`
</output>

---
phase: 07-file-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/__tests__/TemplatePickerModal.test.tsx
  - client/src/lib/__tests__/templateTokens.test.ts
  - client/src/lib/__tests__/builtInTemplates.test.ts
  - server/tests/routes/templates.test.ts
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03

must_haves:
  truths:
    - "Four test files exist with failing (RED) tests that establish the behavioral contracts for all three template requirements"
    - "Client token substitution behavior is specified: {{date}} → YYYY-MM-DD, {{title}} → filename without .md extension"
    - "Built-in template structure is specified: daily-note, meeting-note, decision-record each have required YAML frontmatter and heading patterns"
    - "Server template CRUD behavior is specified: GET returns list, POST creates file, 400 on blank name"
    - "TemplatePickerModal step 1 behavior is specified: blank option present, built-ins listed, selecting blank advances to step 2 with empty content"
  artifacts:
    - path: "client/src/__tests__/TemplatePickerModal.test.tsx"
      provides: "Component test stubs for template picker modal (TMPL-03)"
    - path: "client/src/lib/__tests__/templateTokens.test.ts"
      provides: "Unit test stubs for applyTokens function (TMPL-01)"
    - path: "client/src/lib/__tests__/builtInTemplates.test.ts"
      provides: "Unit test stubs for BUILT_IN_TEMPLATES constant (TMPL-01)"
    - path: "server/tests/routes/templates.test.ts"
      provides: "Integration test stubs for GET/POST /api/templates (TMPL-02)"
  key_links:
    - from: "client/src/__tests__/TemplatePickerModal.test.tsx"
      to: "client/src/components/FolderPickerModal.tsx"
      via: "import of FolderPickerModal — fails RED until Wave 1 adds template step"
    - from: "server/tests/routes/templates.test.ts"
      to: "server/src/routes/templates.ts"
      via: "buildApp + route registration — fails RED until Wave 1 creates route"
---

<objective>
Create Wave 0 failing test stubs that establish behavioral contracts for all template requirements.

Purpose: Ensure RED state before any implementation. Tests define the exact API shapes, token substitution rules, built-in template structures, and modal step behavior that Wave 1 plans must satisfy.
Output: 4 test files in RED state. Running the test suite fails with import errors or assertion failures.
</objective>

<execution_context>
@/Users/romankarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romankarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-file-templates/07-RESEARCH.md
@.planning/phases/07-file-templates/07-VALIDATION.md

<interfaces>
<!-- Key interfaces the test stubs will import against — these do NOT exist yet (Wave 0 = RED). -->

Files to be created by Wave 1 (imports will fail RED until then):

client/src/lib/templateTokens.ts (Wave 1):
```typescript
export function applyTokens(content: string, vars: { title: string; date: string }): string
```

client/src/lib/builtInTemplates.ts (Wave 1):
```typescript
export interface TemplateEntry { id: string; label: string; content: string; }
export const BUILT_IN_TEMPLATES: TemplateEntry[]  // 3 items: daily-note, meeting-note, decision-record
```

client/src/components/FolderPickerModal.tsx (existing, to be modified in Wave 1):
```typescript
// Current onConfirm: (filePath: string, fileName: string) => void
// After Wave 1: (filePath: string, fileName: string, content: string) => void
// Wave 0 tests import the component directly — they will render RED behavior
```

server/src/routes/templates.ts (Wave 1):
// GET /api/templates → { templates: Array<{ name: string; content: string }> }
// POST /api/templates { name: string; content: string } → 201 { name: string }
// 400 { error: 'Invalid name' } if name is blank/invalid

Existing server test pattern (server/tests/routes/images.test.ts):
```typescript
import { buildApp } from '../../src/app.js';
import type { FastifyInstance } from 'fastify';
let tmpDir: string;
let app: FastifyInstance;
beforeEach(async () => {
  tmpDir = await fs.mkdtemp(path.join(os.tmpdir(), 'marky-templates-test-'));
  app = await buildApp({ rootDir: tmpDir });
});
afterEach(async () => {
  await app.close();
  await fs.rm(tmpDir, { recursive: true, force: true });
});
```

Existing client test pattern (client/src/hooks/__tests__/useTabPersistence.test.ts):
// Uses vitest + @testing-library/react, imports from relative paths
// Wave 0 comment at top: "// Wave 0 RED tests — X does not exist yet."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Client lib test stubs (templateTokens + builtInTemplates)</name>
  <files>client/src/lib/__tests__/templateTokens.test.ts, client/src/lib/__tests__/builtInTemplates.test.ts</files>
  <behavior>
    templateTokens.test.ts:
    - Test: applyTokens('Hello {{title}}', { title: 'my-note', date: '2026-03-11' }) === 'Hello my-note'
    - Test: applyTokens('Date: {{date}}', { title: 'x', date: '2026-03-11' }) === 'Date: 2026-03-11'
    - Test: applyTokens('{{title}} on {{date}}', { title: 'meeting', date: '2026-03-11' }) === 'meeting on 2026-03-11'
    - Test: applyTokens replaces multiple occurrences of same token (global replace)
    - Test: applyTokens strips .md extension from title — 'my-note.md' title becomes 'my-note' in output

    builtInTemplates.test.ts:
    - Test: BUILT_IN_TEMPLATES has exactly 3 entries
    - Test: each entry has id, label, content fields (non-empty strings)
    - Test: daily-note entry has id 'daily-note' and content contains '{{date}}'
    - Test: meeting-note entry has id 'meeting-note' and content contains '{{title}}'
    - Test: decision-record entry has id 'decision-record' and content contains '{{date}}' and '{{title}}'
  </behavior>
  <action>
    Create the `client/src/lib/__tests__/` directory if it doesn't exist.

    Create `client/src/lib/__tests__/templateTokens.test.ts`:
    - Header comment: "// Wave 0 RED tests — client/src/lib/templateTokens.ts does not exist yet."
    - Import `applyTokens` from `'../templateTokens'` (will fail RED)
    - Write 5 test cases covering the behaviors above
    - The .md extension strip test: call applyTokens with title 'my-note.md' and verify output heading does not contain '.md'

    Create `client/src/lib/__tests__/builtInTemplates.test.ts`:
    - Header comment: "// Wave 0 RED tests — client/src/lib/builtInTemplates.ts does not exist yet."
    - Import `BUILT_IN_TEMPLATES` from `'../builtInTemplates'` (will fail RED)
    - Write 5 test cases covering the behaviors above
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky/client && npm test -- --run 2>&1 | grep -E "(FAIL|failed|cannot find|Cannot find)" | head -10</automated>
  </verify>
  <done>Both files exist with failing tests (import errors or assertion failures). Test runner reports failures for these two files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client component test stub (TemplatePickerModal)</name>
  <files>client/src/__tests__/TemplatePickerModal.test.tsx</files>
  <behavior>
    - Test: FolderPickerModal renders a template selection step (step 1) containing a "Blank" option
    - Test: all 3 built-in template labels appear in step 1 (Daily Note, Meeting Note, Decision Record)
    - Test: clicking "Blank" advances to step 2 (location step — filename input appears)
    - Test: clicking a built-in template advances to step 2 with that template's content stored
    - Test: template step has a modal title (e.g. "New File" or "Choose template")
  </behavior>
  <action>
    Create `client/src/__tests__/TemplatePickerModal.test.tsx`:
    - Header comment: "// Wave 0 RED tests — FolderPickerModal template step does not exist yet."
    - Imports: vitest describe/it/expect, @testing-library/react render/screen/fireEvent, FolderPickerModal from '../components/FolderPickerModal'
    - Mock `fetch` with vi.fn() returning an empty tree: `{ items: [] }`
    - Write 5 tests covering the behaviors above

    The existing modal structure renders a single step (filename + folder). These tests reference template step content that doesn't exist yet — they will fail RED with "Unable to find element" errors.

    Test for "Blank" advancing: fire click on "Blank" button, then assert a filename input is now visible.
    Test for built-in label: assert text "Daily Note" is visible on initial render.
    Test for built-in click advancing: fire click on "Meeting Note", assert filename input visible.

    Use `beforeEach` to set up the fetch mock and `afterEach` to restore with `vi.restoreAllMocks()`.
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky/client && npm test -- --run 2>&1 | grep -E "TemplatePickerModal" | head -10</automated>
  </verify>
  <done>TemplatePickerModal.test.tsx exists with failing tests. Test runner reports failures for this file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Server route test stub (templates CRUD)</name>
  <files>server/tests/routes/templates.test.ts</files>
  <behavior>
    - Test: GET /api/templates returns 200 with { templates: [] } when .marky/templates/ dir is empty
    - Test: POST /api/templates with { name: 'my-tmpl', content: '# Hello' } returns 201 { name: 'my-tmpl' } and file exists on disk
    - Test: GET /api/templates after POST returns the saved template in the list
    - Test: POST /api/templates with blank name returns 400 { error: 'Invalid name' }
    - Test: GET /api/templates returns 200 even when .marky/templates/ dir doesn't exist yet (auto-creates)
  </behavior>
  <action>
    Create `server/tests/routes/templates.test.ts`:
    - Header comment: "// Wave 0 RED tests — server/src/routes/templates.ts does not exist yet."
    - Use the established server test pattern:
      - `import fs from 'fs/promises'; import path from 'path'; import os from 'os';`
      - `import { buildApp } from '../../src/app.js';`
      - `let tmpDir: string; let app: FastifyInstance;`
      - `beforeEach`: mkdtemp + buildApp
      - `afterEach`: app.close + fs.rm(tmpDir, recursive)
    - Write 5 tests covering the behaviors above
    - Tests will fail RED with 404 errors (route not registered) until Wave 1 creates the route and registers it

    For the POST test that verifies file on disk: check `fs.access(path.join(tmpDir, '.marky/templates/my-tmpl.md'))` resolves without throwing.
  </action>
  <verify>
    <automated>cd /Users/romankarski/projects/portal-hub/Marky/server && npm test -- --run 2>&1 | grep -E "templates" | head -10</automated>
  </verify>
  <done>server/tests/routes/templates.test.ts exists with failing tests. Server test runner reports failures for this file.</done>
</task>

</tasks>

<verification>
After all three tasks:

1. Full client test suite run: `cd /Users/romankarski/projects/portal-hub/Marky/client && npm test -- --run` — should show failures for `templateTokens`, `builtInTemplates`, `TemplatePickerModal` test files
2. Server test suite run: `cd /Users/romankarski/projects/portal-hub/Marky/server && npm test -- --run` — should show failures for `templates.test.ts`
3. All 4 stub files exist at their expected paths
4. No other previously-passing tests are broken (grep for pass counts before/after)
</verification>

<success_criteria>
- 4 Wave 0 test files exist with RED (failing) tests
- Client suite fails on exactly the 3 new files, all other client tests still pass
- Server suite fails on templates.test.ts, all other server tests still pass
- Behavioral contracts established for TMPL-01 (token substitution + built-in structure), TMPL-02 (server CRUD), TMPL-03 (modal step rendering)
</success_criteria>

<output>
After completion, create `.planning/phases/07-file-templates/07-01-SUMMARY.md`
</output>

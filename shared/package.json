{
  "name": "@marky/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/types.ts",
  "types": "dist/types.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly --outDir dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
